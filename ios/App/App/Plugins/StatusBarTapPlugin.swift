import Capacitor
import UIKit

@objc(StatusBarTapPlugin)
public class StatusBarTapPlugin: CAPPlugin {

    // Intentionally empty — swizzling is in AppDelegate where the bridge is ready.
    // This class exists so Capacitor can discover it as a plugin.
}
